# Contributing

Thanks for helping improve the Kaggle Grandmaster Playbook.

This project is especially interested in making AI agents better at autonomous Kaggle research: reading a competition, inferring likely solution families, retrieving historical evidence, and producing useful experiments.

Contributions should prefer measurable improvements over prompt-only claims. Please feel free to fork, simplify, rebuild, or connect the workflow to a different agent runtime, LLM API, crawler or vector store. The core skill must remain usable without a specific execution harness.

## Good Issue Reports

Please include:

- Competition profile or slug.
- Query or command you ran.
- Expected historical cases.
- Actual historical cases.
- Why the ranking or output should change.

Example:

```text
Profile: audio multi-label BirdCLEF-style competition, weak labels, AUC metric.
Expected: BirdCLEF SED / spectrogram / overlap inference cases near the top.
Actual: generic CV cases ranked above audio cases.
Reason: audio modality should dominate; CV should appear only as spectrogram analogy.
```

## Useful Pull Requests

- Add or improve domain, metric, risk, and stage inference in `scripts/grandmaster_core.py`.
- Improve hybrid retrieval and reranking with a benchmark case proving the change.
- Add data profilers in `scripts/profile_competition.py`.
- Improve experiment scheduling, isolation, or reflection in `scripts/competition_memory.py`.
- Improve leakage/data-usage checks in `scripts/audit_pipeline.py` with positive and negative fixtures.
- Add benchmark profiles that make retrieval quality measurable.
- Add adapters for Kaggle CLI metadata, downloaded kernels, discussion/write-up exports, or notebook-to-Python conversion.
- Add tests or reproducible examples for bad retrieval cases.
- Improve `references/search-rules.md` and `references/output-schema.md`.
- Add cleaner book export and sanitization workflows.
- Add examples of new competition collection cards generated by `scripts/collect_competition_experience.py`.
- Improve post-competition distillation, review gates, learned-card schemas or negative-experience retrieval with tests.

## Data And Safety

- Do not submit private API keys, Kaggle credentials, cookies, or local account files.
- Do not include private absolute paths in public book exports.
- Keep source attribution when adding new cases.
- Respect Kaggle rules, notebook licenses, and dataset licenses.

## Development Checks

```text
python -m compileall -q scripts tests
python -m unittest discover -s tests -v
python scripts/evaluate_retrieval.py --strict
python scripts/evaluate_skill.py --strict
python scripts/skill_doctor.py --strict
python scripts/build_knowledge_manifest.py --check
```

If you use this as a Codex/OpenAI skill, validate the skill folder with your local skill validator.
